# Goal 执行日志：文章发布工具链（2026-07-19）

## 目标

按照 `ARTICLE_PUBLISHING_PLAN_2026-07-18.md` 实现并验证最小文章发布闭环：原稿导入、内容校验、草稿隔离、真实样式预览和发布前完整构建。

## 本轮边界

- 不修改 `G:\ChenTingToDo-文章归档` 中的 7 篇正式文章。
- 不把测试文章保留在正式文章目录。
- 不执行 Git 提交、推送或 Vercel 部署。
- 不覆盖当前工作区中属于 CI、Node.js 和依赖监控任务的未提交改动。
- 不引入 CMS、数据库、登录系统或新的运行时依赖。

## 验收标准

- [x] 中文和空格路径的原稿能够导入为默认草稿。
- [x] 同名 slug 不会覆盖已有草稿或文章。
- [x] 元数据、标题层级、危险 HTML、空链接、失效相对链接和明显密钥会被校验。
- [x] 生产读取只接受 `published: true`，草稿不会进入列表和搜索索引。
- [x] 指定草稿可在本地使用正式文章页样式预览。
- [x] 发布失败能恢复原草稿，不留下半发布文件或旧 `out` 产物。
- [x] 发布成功前会执行项目完整检查与 Pagefind 构建。
- [x] 临时测试文章完成端到端验收后被清理。

## 进度

- [x] 核对计划书、仓库和并行未提交改动。
- [x] 实现模板、导入、校验、预览和发布命令。
- [x] 增加自动测试。
- [x] 使用临时文章执行草稿隔离与浏览器预览。
- [x] 使用临时文章执行发布和生产构建验收。
- [x] 清理测试文章并记录最终结果。

## 实际验证结果

1. `npm test`：6 个测试全部通过，覆盖中文空格路径导入、禁止覆盖、缺失元数据、结构与安全校验、失败回滚和成功发布。
2. 草稿生产隔离：临时草稿存在时运行 `npm run build`，构建仍只有原 4 篇文章；临时 slug 不存在于路由和 Pagefind 输出。
3. 草稿真实预览：`npm run article:preview -- article-pipeline-e2e -- -p 4174` 可打开正式文章页样式；文章列表不包含草稿，新浏览器页无控制台错误。
4. 发布闭环：`npm run article:publish -- article-pipeline-e2e` 完整通过 lint、TypeScript、测试、内容校验、Next.js 静态构建和 Pagefind。
5. 生产产物：临时文章生成静态页面，并能通过站内搜索“临时验收”找到；页面无控制台错误。
6. 清理复验：删除临时正式文章后再次运行 `npm run check` 全部通过；最终静态产物只有原 4 篇文章，未残留临时文章或索引。

## 最终状态

- 7 篇正式归档文章未修改。
- 没有执行 Git 提交、推送或线上部署。
- 当前只保留发布工具、三类模板、自动测试、中文使用说明和本执行日志。
